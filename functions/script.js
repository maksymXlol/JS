// debugger;
let x = add(5, 6);

let addExp = function (a, b) {
  return a + b;
};

let addArr = (a, b) => a + b;

let isEven = (number) => {
  return number % 2 == 0;
};

function displayMessage(message) {
  alert(message);
}

function add(a, b) {
  let sum = a + b;
  return sum;
}

let sum = addExp;
